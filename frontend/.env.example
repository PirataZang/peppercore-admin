# URL base da API do backend
# Em desenvolvimento local, deixe vazio para usar o proxy do Vite (/api -> http://backend:8000)
# Em produção, defina a URL completa do backend (ex: https://api.seudominio.com)
VITE_API_URL=